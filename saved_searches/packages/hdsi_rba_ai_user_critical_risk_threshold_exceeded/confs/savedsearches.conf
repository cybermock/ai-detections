[Threat - HDSI RBA AI User Critical Risk Threshold Exceeded - Rule]
action.correlationsearch.enabled = 1
action.correlationsearch.label = HDSI RBA AI User Critical Risk Threshold Exceeded
action.correlationsearch.annotations = {"mitre_attack": ["T1078"]}
action.customsearchbuilder.enabled = 0
action.customsearchbuilder.spec = {}
action.email = 0
action.notable = 1
action.notable.param.drilldown_dashboards = []
action.notable.param.drilldown_searches = [{"name":"View contributing AI detections","search":"| from datamodel:\"Risk.All_Risk\" where All_Risk.risk_object=\"$risk_object$\" source=\"*HDSI AI*\" NOT source=\"*HDSI RBA*\" | table _time, source, risk_score, risk_object, risk_message | sort - _time","earliest_offset":"$info_min_time$","latest_offset":"$info_max_time$"},{"name":"Summarize user AI risk composition","search":"| from datamodel:\"Risk.All_Risk\" where All_Risk.risk_object=\"$risk_object$\" source=\"*HDSI AI*\" NOT source=\"*HDSI RBA*\" | stats count as event_count sum(risk_score) as total_risk values(source) as contributing_searches by risk_object risk_object_type | eval contributing_searches=mvjoin(contributing_searches, \"; \") | table risk_object, risk_object_type, event_count, total_risk, contributing_searches","earliest_offset":"$info_min_time$","latest_offset":"$info_max_time$"}]
action.notable.param.rule_description = User $user$ accumulated $total_risk$ AI risk points in 24 hours.
action.notable.param.rule_title = AI User Risk Threshold Exceeded - High ($user$)
action.notable.param.security_domain = threat
action.notable.param.severity = high
action.notable.param.verbose = 0
alert.digest_mode = 1
action.risk = 1
action.risk.param._risk = [{"risk_object_field":"user","risk_object_type":"user","risk_score":50}]
action.risk.param._risk_message = AI user critical risk threshold exceeded: $user$ accumulated $total_risk$ risk points in the last 24 hours.
action.risk.param._risk_score = 0
action.risk.param.verbose = 0
alert.managedBy =
alert.suppress = 1
alert.suppress.fields = user
alert.suppress.period = 7200s
alert.track = 1
allow_skew = 5m
counttype = number of events
cron_schedule = 12-59/15 * * * *
description = Create a high notable when user AI risk reaches critical threshold in 24 hours.
disabled = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
dispatch.rt_backfill = 1
dispatch.ttl = 2p
enableSched = 1
realtime_schedule = 0
relation = greater than
quantity = 0
request.ui_dispatch_app = SplunkEnterpriseSecuritySuite
schedule_window = auto
search = index=risk earliest=-24h@h latest=now search_name="*HDSI AI*" risk_object_type="user" \
	| where NOT match(search_name,"HDSI RBA") \
	| stats sum(risk_score) as total_risk values(search_name) as detections min(_time) as firstTime max(_time) as lastTime by risk_object \
	| eval user=risk_object \
	| eval control_name="high_risk_threshold" \
	| lookup ai_detection_config control_name OUTPUTNEW value as high_risk_threshold \
	| eval high_risk_threshold=coalesce(tonumber(high_risk_threshold),90) \
	| where total_risk>=high_risk_threshold \
	| `hdsi_rba_ai_user_critical_risk_threshold_exceeded_filter` \
	| `hdsi_rba_ai_user_critical_risk_threshold_exceeded_customizations`
