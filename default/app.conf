[install]
state = enabled
build = 1
install_source_checksum = auto

[ui]
is_visible = true
label = ES AI RBA Starter Pack

[launcher]
author = Brainstorm SOC
description = 40+ correlation searches, macros, and lookups for detecting unsanctioned AI usage via RBA in Splunk ES. Covers web access, CLI tools, desktop apps, API calls, local LLMs, DLP violations, and behavioral anomalies across 7 MITRE ATT&CK techniques.
version = 1.0.0
min_splunk_version = 9.0.0

[package]
id = splunk_es_ai_rba
check_for_updates = false

